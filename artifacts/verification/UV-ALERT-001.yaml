artifacts:
  - id: UV-ALERT-001
    type: unit-verification
    title: "Unit tests for SWDD-ALERT-001"
    status: implemented
    description: >
      Automated unit tests verifying alert dispatcher dedup and rate limiting.
      Run via: cargo test -p wohl-alert
    tags: [testing, unit]
    fields:
      method: automated-test
      steps:
        - run: cargo test -p wohl-alert
    links:
      - type: verifies
        target: SWDD-ALERT-001
