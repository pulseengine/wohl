[package]
name = "wohl-leak"
description = "Wohl Water Leak Detector — verified safety-critical leak detection"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
rust-version = "1.85.0"

[lib]
path = "plain/src/lib.rs"
crate-type = ["rlib"]
